---
name: init
description: Guided looptech project setup — Project Profile, database, agent roles, memory vault, runtimes.
---

Load and follow the skill `init` in this plugin (`skills/init/SKILL.md`).

Run the six-step onboarding: detect host, write the Project Profile to both `CLAUDE.md` and `AGENTS.md`, configure database, map agent roles from the host catalog, offer memory-graph, validate runtimes.
